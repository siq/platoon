daemon:
  detached: false
components:
  - platoon.TaskQueue
configuration:
  platoon.idler.Idler:
    timeout: 5
  schema:platoon:
    admin_url: postgresql://postgres@locahost/postgres
    hstore: true
    url: postgresql://postgres@localhost/platoon
logging:
  disable_existing_loggers: false
  formatters:
    standard:
      (): spire.support.logs.LogFormatter
  handlers:
    stream:
      class: logging.StreamHandler
      formatter: standard
  root:
    level: DEBUG
    handlers: [stream]
